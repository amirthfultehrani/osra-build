*** Begin Patch
*** Update File: main.cc
@@
#include <stdlib.h>
#include <errno.h>
+#ifdef _WIN32
+#include <direct.h>          /* for _mkdir */
+/* map POSIX mkdir(path,mode) to Windows _mkdir(path) */
+#define mkdir(path,mode) _mkdir(path)
+#endif
***
*** End Patch
